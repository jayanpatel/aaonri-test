import { NumFormatDirective } from './num-format.directive';

describe('NumFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new NumFormatDirective();
    expect(directive).toBeTruthy();
  });
});
